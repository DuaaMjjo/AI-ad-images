_Install the required packages by the following command:_
```bash
pip install -r requirements.txt
```

_Create the environment file `.env` with the following contents:_
```text
OPENAI_API_KEY=
REPLICATE_API_TOKEN =
```

